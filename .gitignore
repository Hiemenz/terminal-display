*.pyc
__pycache__/
data/
.env

# Runtime config backups / atomic-write temp files
config/config.yaml.bak
config/.config.*.tmp

# Rendered output images (generated at runtime)
output/*.bmp
output/*.png

# Gallery uploads (stored on device, not in repo)
assets/gallery/

# Poetry virtualenv if created inside project
.venv/
